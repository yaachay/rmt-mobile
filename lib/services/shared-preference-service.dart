import 'package:rakhine_myanmar_translator/models/models.dart';
import 'package:shared_preferences/shared_preferences.dart';

class SharedPreferenceService {
  // Future setSetting(Setting setting) async {
  //   final SharedPreferences prefs = await SharedPreferences.getInstance();
  //   await prefs.setString('lang', setting.lang.toString());
  //   await prefs.setString('theme', setting.theme.toString());
  // }

  // Future<Setting> getSetting() async {
  //   final SharedPreferences prefs = await SharedPreferences.getInstance();
  //   Languages lang = prefs.getString('lang') as Languages;
  //   Themes theme = prefs.getString('theme') as Themes;
  //   return Setting(lang: lang, theme: theme);
  // }
}
